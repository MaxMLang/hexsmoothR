# Compute weights for N orders (internal function)

Compute weights for N orders (internal function)

## Usage

``` r
compute_weights_n_orders(
  neighbor_orders,
  sigma,
  center_weight,
  weights_input,
  avg_dist,
  adaptive_sigma_factor
)
```
