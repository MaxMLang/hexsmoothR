# C++ wrapper for N-order smoothing (internal)

C++ wrapper for N-order smoothing (internal)

## Usage

``` r
process_district_all_vars_n_orders_wrapper(
  variable_values,
  neighbors,
  weights,
  hex_indices,
  var_names
)
```
