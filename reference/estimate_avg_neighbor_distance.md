# Estimate average distance between first-order neighbours (internal)

Estimate average distance between first-order neighbours (internal)

## Usage

``` r
estimate_avg_neighbor_distance(grid_proj, first_neighbors, sample_size)
```
