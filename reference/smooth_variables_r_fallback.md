# R fallback smoothing function (internal)

R fallback smoothing function (internal)

## Usage

``` r
smooth_variables_r_fallback(
  variable_values,
  neighbors,
  weights,
  hex_indices,
  var_names
)
```
