# Internal verbose-print helper

Honours \`options(hexsmoothR.verbose = ...)\`. Use this instead of bare
\`cat()\` so users can silence the package globally.

## Usage

``` r
hex_msg(...)
```

## Arguments

- ...:

  Passed to \`cat()\`.
