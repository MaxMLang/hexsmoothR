# Actually make the grid for one area

Actually make the grid for one area

## Usage

``` r
create_single_grid(
  study_area,
  grid_size,
  type,
  projection_crs,
  return_crs,
  area_id = NULL,
  check_size = TRUE,
  max_cells = 1e+06
)
```
