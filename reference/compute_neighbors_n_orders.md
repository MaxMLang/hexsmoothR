# Compute neighbours for N orders (internal, vectorised set-based BFS)

Compute neighbours for N orders (internal, vectorised set-based BFS)

## Usage

``` r
compute_neighbors_n_orders(grid_proj, neighbor_orders)
```
