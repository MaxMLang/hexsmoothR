# C++ wrapper for 2-order smoothing (internal)

C++ wrapper for 2-order smoothing (internal)

## Usage

``` r
process_district_all_vars_wrapper(
  variable_values,
  first_neighbors,
  second_neighbors,
  weights,
  hex_indices,
  var_names
)
```
