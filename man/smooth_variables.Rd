% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpp_wrapper.R
\name{smooth_variables}
\alias{smooth_variables}
\title{Apply spatial smoothing to variables on a hexagonal grid}
\usage{
smooth_variables(
  variable_values,
  neighbors,
  weights,
  hex_indices = NULL,
  var_names = NULL
)
}
\arguments{
\item{variable_values}{Named list of numeric vectors (one per variable).}

\item{neighbors}{List of neighbour lists (per order). Either the `neighbors`
element of a topology, or the topology object itself.}

\item{weights}{List with `center_weight` and `neighbor_weights`. Either the
`weights` element of a topology, or the topology object itself.}

\item{hex_indices}{Integer vector of cell indices to process. Defaults to
all cells.}

\item{var_names}{Character vector of variable names. Defaults to
`names(variable_values)`.}
}
\value{
Named list with one entry per variable. Each entry contains:
  \itemize{
    \item `raw`: the original (unsmoothed) centre-cell values
    \item `weighted_combined`: weighted average of centre + all neighbours
    \item `neighbors_<N><suffix>`: mean of neighbours at order N
      (e.g. `neighbors_1st`, `neighbors_2nd`, `neighbors_3rd`)
  }
}
\description{
Smooths variables using the topology produced by [`compute_topology()`].
Uses the compiled C++ implementation when available; if the C++ call
fails for an unexpected reason, falls back to the pure-R implementation
and re-raises clear validation errors.
}
